フォロー一覧
<% @users.each do |user|%>
  <%= image_tag user.post.get_post_image(100,100)%>
  <%= user.name %>
  <%= user.introduction %>
  <% if current_user.following?(@user) %>
          <%= link_to "フォローをやめる", user_relationships_path(@user.id), method: :delete %>
  <% end %>
<% end %>